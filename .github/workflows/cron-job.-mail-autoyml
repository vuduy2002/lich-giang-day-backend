name: Run Every Minute Vercel Job

on:
  schedule:
    # Chạy mỗi phút
    - cron: '* * * * *'
 
  
jobs:
  run-vercel-api:
    runs-on: ubuntu-latest

    steps:
      - name: Call Vercel API
        run: |
          curl -X GET https://lich-giang-day-backend.vercel.app/api/notifications
